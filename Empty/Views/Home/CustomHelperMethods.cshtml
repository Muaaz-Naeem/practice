@using Empty.CustomHelpers
@{
    ViewBag.Title = "CustomHelperMethods";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Custom Helper Methods</h2>

<table class="table table-striped table-hover col-md-6 ">
    <thead class="thead-inli">
        <tr>
            <th class="col-md-2"> </th>
            <th class="col-md-4"> </th>
        </tr>

    </thead>
    <tbody>
        <tr>
            <td style="font-weight:bold">UserName </td>
            <td>
                @Html.Custom("", "text", "form-control", "Username")
            </td>
        </tr>
        <tr>
            <td style="font-weight:bold">Password </td>
            <td>
                @Html.Custom("", "text", "form-control", "password")
            </td>
        </tr>
        <tr>
            <td style="font-weight:bold">Date of Birth </td>
            <td>
                @Html.Custom("", "date", "form-control", "")
            </td>
        </tr>

        <tr>
            <td style="font-weight:bold"> Color</td>
            <td>
                @Html.Custom("", "color", "", "")
            </td>
        </tr>

    </tbody>
</table>